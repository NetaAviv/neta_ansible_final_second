---
- name: Install Nginx (Amazon Linux 2023)
  ansible.builtin.dnf:
    name: nginx
    state: present

- name: Enable and Start Nginx Service
  ansible.builtin.systemd:
    name: nginx
    enabled: yes
    state: started
